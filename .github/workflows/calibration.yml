name: calibration

on:
  schedule:
    - cron: "23 5 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: calibration
  cancel-in-progress: false

jobs:
  pinned-corpus:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - name: Measure and verify pinned corpus
        run: sh scripts/calibrate-corpus.sh calibration-results
      - name: Preserve reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: calibration-reports
          path: calibration-results
          if-no-files-found: warn
          retention-days: 30

  performance-budget:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"
      - name: Measure cold and warm runs
        run: sh scripts/measure-performance.sh performance.json
      - name: Preserve timing samples
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: performance-measurements
          path: performance.json
          if-no-files-found: warn
          retention-days: 30
